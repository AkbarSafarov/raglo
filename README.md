# Raglo — интернет-магазин сантехники

Премиальный e-commerce сайт для продажи смесителей и душевых систем. Многостраничный статический фронтенд с современным инструментарием сборки.

## Стек технологий

| Слой | Инструменты |
|------|-------------|
| Сборка | Gulp 5, Babel 7 |
| Стили | SCSS (Dart Sass), Bootstrap 5.3, Autoprefixer |
| JavaScript | jQuery, Vanilla JS (ES6+) |
| UI-компоненты | Swiper.js, Lightgallery, Tippy.js, Bootstrap Icons |

## Страницы

- **Главная** (`index.html`) — hero-слайдер, категории товаров
- **Каталог** (`catalog.html`) — листинг с фильтрацией и пагинацией
- **Карточка товара** (`card.html`) — галерея, характеристики, выбор цвета
- **Корзина** (`cart.html`, `cart-2.html`) — управление позициями, промокод
- **Оформление заказа** (`order.html`) — способы оплаты, доставка
- **Сравнение** (`comparison.html`)
- **Личный кабинет** (`account.html`)
- **Контакты** (`contacts.html`)

## Структура проекта

```
raglo/
├── src/
│   ├── html/
│   │   ├── pages/      # Шаблоны страниц
│   │   └── blocks/     # Переиспользуемые блоки (шапка, подвал и др.)
│   ├── styles/         # SCSS-модули
│   ├── js/             # JavaScript-модули
│   ├── files/          # Изображения и медиа
│   └── libs/           # Сторонние библиотеки
├── build/              # Результат dev-сборки
├── dist/               # Результат prod-сборки
└── gulpfile.mjs        # Конфигурация Gulp
```

## Запуск

```bash
npm install

# Режим разработки (live-reload через BrowserSync)
npm run dev

# Продакшн-сборка (минификация → dist/)
npm run build
```

## Дизайн-система

- **Основные цвета:** красный `#e20013`, чёрный `#191415`, слоновая кость `#f2f1f0`
- **Типографика:** Manrope, Onest, Inter, Open Sans, Roboto
- **Адаптивность:** mobile-first, Bootstrap-сетка
